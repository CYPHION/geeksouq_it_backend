/**
 * Shared directory names.
 *
 * Single source of truth for the folder where multer stores uploads and
 * where utils/function.js resolves file paths from.
 */
exports.uploadDirectory = 'uploads';
